{
  "_comment": "Committed + shared by both machines. Required by scripts/start-box and scripts/console. brain.executorHost = the box's LAN IP works from the laptop AND the box (the box reaches its own IP; executor binds 0.0.0.0). bridgeDir MUST match BRIDGE_DIR in mqbridge/bridge.lua. Env vars (EQ_*) still override per-machine if ever needed.",

  "bridgeDir": "C:/EQ/.bridge",

  "_relay_comment": "How the executor relays a command to a named client. {peer}=target name, {cmd}=command to run on it. DanNet's /dex bypasses the client's #/^ chat-command routing (server cmds aren't parsed), so we use e3next's targeted broadcast, which runs the command locally on the peer. Change here if your e3 targeted-broadcast command differs.",
  "relay": "/e3bct {peer} {cmd}",

  "playerStateFile": "C:/EQ/.player/state.tsv",

  "_shadow_comment": "Phase 1 shadow loop (brain/shadow.js). squads = client(s) that auto-teleport into your zone when you zone. sendCoords = also #goto to your exact loc on arrival (verify #goto coord order first).",
  "shadow": { "squads": ["Oram"], "sendCoords": false },

  "_roster_comment": "FILL THIS IN. role is one of: tank, healer, cc, support, dps. Mode B drops the bot whose ROLE matches the player's role (not class). anchor = the Lt that issues Mode A invites. essentialClasses are never dropped (a Cleric must always remain) UNLESS the player is that class; when the role-match would drop an essential, we keep it and drop a redundant bot by dropPriority order.",
  "roster": {
    "player": { "name": "", "class": "", "role": "" },
    "guildLeader": "",
    "anchor": "Oram",
    "essentialClasses": ["Cleric"],
    "dropPriority": ["dps", "support", "cc", "healer", "tank"],
    "lieutenants": [
      { "name": "Oram",   "class": "", "role": "", "bots": [] },
      { "name": "Cynric", "class": "", "role": "", "bots": [] }
    ]
  },

  "executor": {
    "_comment": "How the executor (on the 32 GB box) binds. host 0.0.0.0 = reachable across the LAN.",
    "host": "0.0.0.0",
    "port": 8777
  },

  "brain": {
    "_comment": "Where the brain (laptop) sends intents. Set executorHost to the BOX's LAN IP for cross-LAN; 127.0.0.1 if testing on one machine.",
    "executorHost": "192.168.1.204",
    "executorPort": 8777
  }
}
